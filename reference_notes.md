# Supplied Reference Findings

The supplied web reference source defines caption templates with word-aware timing, natural-pause grouping, per-word active highlighting, and template-specific effects such as pop, karaoke, bounce, typewriter, slide-up, and fade. The SubReel Worker now requests optional per-word timing and Flutter falls back to estimated word timings for older transcripts.

The supplied editor screenshots and recording establish the requested workspace hierarchy: a large clean preview remains the visual focus; the lower editor is compact; the timeline must not have an enclosing rounded card, left/right rails, or oversized generic gray blocks. The desired strip order is a connected reel of small video thumbnails, a thin audio row, and caption segments immediately below, with a single synchronized playhead.

These findings are design references only. SubReel should retain its own dark black-and-white identity and must not copy external branding or artwork.

The later supplied recording separates base caption templates from fine Style controls. Template selection should update the main preview immediately, while Style opens a dedicated sheet for size, position, color, and weight. The user requested a vertically scrollable template gallery rather than a horizontal strip, with SubReel retaining original visual treatment and English UI labels.
